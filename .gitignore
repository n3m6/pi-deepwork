# pi-deepwork: per-run pipeline state and telemetry. Never commit.
.pipeline/

# Editor / OS noise
.DS_Store
.vscode/
.idea/

# Node (only needed for `npm test`; should not exist in CI either)
node_modules/
npm-debug.log*
*.log
