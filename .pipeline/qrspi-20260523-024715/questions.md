# Research Questions

### Q1: How does the existing opencode deepwork orchestrator at `/home/n3m6/.config/opencode/agents/deepwork.md` define the 10 pipeline stages (ordering, inputs, dispatch contracts), determine the route (`full` vs `quick-fix`), and prescribe the backward loop protocol and `state.md`-based recovery contract?
**Tag**: `codebase`
**Covers**: `FR-3 [pipeline stages]; FR-4 [file-based state protocol]; FR-9 [quick-fix route]; FR-10 [backward loop protocol]; C-2 [pipeline directory convention]; C-6 [10 stages + skips + backward loops]; NFR-1 [clean state recovery]; AC-3 [quick-fix skips stages]; AC-4 [backward loop replan]; AC-5 [error recovery state]`
**Answer shape**: A table mapping each of the 10 pipeline stages to (a) whether a human gate fires, (b) the route-skip condition, (c) the subagent dispatched, (d) key inputs passed, and (e) the post-return actions (state.md update, telemetry event, git checkpoint). Plus a summary of the route-determination criteria from Stage 1, the backward loop decision flow (which stages can trigger it, the user-prompted options, artifact changes), and the exact `state.md` YAML schema with field definitions. Stop when all stages, route branches, loop paths, and the full state.md schema are catalogued from the single source file.
**Decision unblocked**: Designing the orchestrator skill for pi — what the orchestrator does at each stage boundary, how routes are chosen, how backward loops are handled, and what recovery state to persist.

### Q2: How does the existing opencode deepwork orchestrator handle subagent dispatch mechanics (the `Invoke <agent> as a subagent` pattern including prompt format), return contract parsing (`### Status`, `### Files Written`, `### Route`, `### Backward Loop Request`, `### Telemetry`, `### Summary`), telemetry event emission (JSONL schema, `run-log.md` regeneration, `metrics-summary.md` generation), and git checkpoint integration at each stage boundary?
**Tag**: `codebase`
**Covers**: `FR-5 [orchestrator dispatches stage subagents]; FR-11 [git branch per run]; FR-12 [telemetry events, run-log, metrics-summary]; C-7 [foreground blocking dispatch]; C-8 [branch naming qrspi/<runId>]; C-10 [git availability fallback]; NFR-5 [telemetry artifacts]; NFR-6 [sequential foreground dispatch]; AC-6 [stage artifacts produced]`
**Answer shape**: A structured reference listing (a) the exact subagent invocation prompt template used by the orchestrator (the `=== RUN ID ===` / `=== USER TASK ===` / `=== ROUTE ===` / `=== PHASE DIR ===` headers and their stage-specific combinations), (b) the set of return contract headers parsed and the logic branches each triggers, (c) the full telemetry event schema (all event types — `run.*`, `stage.*`, `gate.*`, `backward_loop.*`, `checkpoint.*` — with their required fields), (d) the `run-log.md` regeneration template (6-section layout), (e) the `metrics-summary.md` structure, and (f) the exact git commands and commit messages used at stage boundaries and pre-flight. Also capture any explicit git-availability detection logic in the source. Stop when every dispatch template, return header, event type, generated-file template, and git command is extracted from the source.
**Decision unblocked**: Adapting the orchestrator's dispatch protocol, return contract parsing, telemetry system, and git checkpointing from opencode patterns to pi equivalents.

### Q3: What is the content, YAML frontmatter structure, system prompt body, subagent-dispatch references, and tool permission patterns of the opencode agent type files located in `/home/n3m6/.config/opencode/agents/` — specifically the qrspi-* agents that correspond to the 55 target agent types?
**Tag**: `codebase`
**Covers**: `FR-8 [~55 agent type files with YAML frontmatter]; C-3 [frontmatter fields: description, tools, model, thinking, max_turns, prompt_mode, extensions]; C-4 [tools and disallowed_tools frontmatter]; C-9 [agent conversion follows mapping tables]`
**Answer shape**: A per-agent inventory table covering every qrspi-* agent file present in `/home/n3m6/.config/opencode/agents/` (file path, YAML frontmatter fields present — description, mode, temperature, steps, permission block — and the first ~50 lines of system prompt body to capture role definition and key rules). Also note any opencode-specific patterns that have no direct pi equivalent (e.g., `permission.task: "qrspi-*"`, `permission.question: allow`, `permission.todowrite: allow`). Stop when all qrspi-* files are inventoried with frontmatter and prompt preamble extracted.
**Decision unblocked**: Porting the 55 agent type prompts from opencode format to pi-subagents-compatible `.md` files using the provided conversion tables.

### Q4: How does pi's extension system support registering slash commands and tools via the `activate(ctx: ExtensionContext)` lifecycle — what is the `ExtensionContext` shape, how are commands registered, how are custom tools registered, and how does the `resources_discover` event with `skillPaths` enable skill injection from `skills/<name>/SKILL.md`?
**Tag**: `web`
**Covers**: `FR-1 [/deepwork command registration]; FR-2 [/deepwork-resume command registration]; FR-7 [qrspi_question tool registration]; NFR-4 [single prompt initiates pipeline]; AC-1 [deepwork starts full pipeline]; AC-2 [deepwork-resume resumes]`
**Answer shape**: A reference table listing each relevant `ExtensionContext` method or event — command registration API (function signature + parameter shape), tool registration API (function signature, parameter schema contract), the `resources_discover` event shape (what fields it carries, specifically `skillPaths`), and the directory convention pi's skill loader uses to resolve `skills/<skill-name>/SKILL.md` paths. Also capture the activate/deactivate lifecycle contract. Stop when the pi extension authoring documentation is exhausted for command, tool, and skill registration patterns.
**Decision unblocked**: Implementing `src/index.ts` — registering the two deepwork commands, the two custom tools, and injecting the deepwork orchestrator skill via the extension lifecycle.

### Q5: What API does the `@tintinweb/pi-subagents` package expose — the `Agent` tool's parameter contract (subagent_type, prompt, description, etc.), the `Symbol.for("pi-subagents:manager")` registration pattern, the `AgentManager` interface (including `spawn()` for background and `spawnAndWait()` for foreground/blocking dispatch), and how subagents return their results?
**Tag**: `web`
**Covers**: `FR-5 [Agent tool dispatch for stage orchestrators]; FR-6 [qrspi_dispatch tool to spawn leaf subagents via Symbol.for]; C-5 [qrspi_dispatch depends on Symbol.for("pi-subagents:manager")]; C-7 [foreground blocking subagents]; NFR-6 [sequential foreground dispatch]; AC-7 [model tier compatibility]`
**Answer shape**: A structured API reference: (a) `Agent` tool parameters — all accepted fields with types, defaults, and descriptions, (b) how the module registers `Symbol.for("pi-subagents:manager")` and the full `AgentManager` TypeScript interface (methods, signatures, return types for `spawn()` and `spawnAndWait()`), (c) the structure and format of subagent return values (what the caller receives after dispatch), and (d) any model tier designations or model selection mechanisms supported. Stop when the pi-subagents README, source types, and documentation exhaust the surfaces needed for dispatching, awaiting, and receiving results from subagents.
**Decision unblocked**: Implementing `src/shared-tools.ts` (the `qrspi_dispatch` tool wrapping `AgentManager`) and the orchestrator's stage subagent dispatch workflow.

### Q6: How does pi-subagents discover agent type `.md` files — the full YAML frontmatter schema (valid fields, accepted values, defaults for `description`, `tools`, `model`, `thinking`, `max_turns`, `prompt_mode`, `extensions`, `disallowed_tools`), file naming conventions, and the directory paths scanned (project-local `.pi/agents/` vs global `~/.pi/agent/agents/`)?
**Tag**: `web`
**Covers**: `FR-8 [agent type files with YAML frontmatter]; C-3 [frontmatter field set]; C-4 [tools/disallowed_tools permission approximation]; AC-7 [model tier assignment per agent type]`
**Answer shape**: A reference table of every supported YAML frontmatter field (field name, type, required/optional, valid values/ranges, default), the file naming convention (extension, naming rules), and the complete directory discovery order (which paths are checked, in what order, how name conflicts are resolved). Include the semantics of `tools: "all"` vs enumerated tool lists, and how `disallowed_tools` interacts with `tools`. Stop when pi-subagents documentation exhausts the agent type file contract.
**Decision unblocked**: Creating and placing the ~55 agent type `.md` files with correct frontmatter and discovering them at runtime.

### Q7: What interactive UI methods does pi's extension runtime context expose via `ctx.ui` — specifically `confirm()` and `select()`, including their full parameter shapes (header, message, options), return types (Promise<boolean> vs Promise<string>), and any constraints on when they can be called during agent execution?
**Tag**: `web`
**Covers**: `FR-7 [qrspi_question tool wrapping ctx.ui]; AC-1 [end-to-end pipeline includes user interaction]`
**Answer shape**: A method-level API reference: `ctx.ui.confirm(params)` — full parameter interface, return type, and description; `ctx.ui.select(params)` — full parameter interface, return type, and description. Also capture any lifecycle constraints (e.g., only available during active tool execution, or only from the main agent context). Stop when the pi extension API documentation for `ctx.ui` is exhausted.
**Decision unblocked**: Implementing the `qrspi_question` tool in `src/shared-tools.ts` that wraps `ctx.ui.confirm()` and `ctx.ui.select()`.

### Q8: How does pi discover and load extensions — the `~/.pi/agent/extensions/` directory convention, the `pi install` command's git URL resolution (including branch/tag syntax and shallow-clone behavior), and the npm-symlink-based installation pathway?
**Tag**: `web`
**Covers**: `NFR-3 [installability via symlink or pi install git]; AC-8 [both installation methods work]`
**Answer shape**: A concise reference covering (a) the exact directory path pi scans for extensions (`~/.pi/agent/extensions/`), the expected structure inside an extension directory (`package.json` shape, entry point convention), (b) the `pi install` CLI syntax for git-based extensions (branch/tag resolution, shallow clone behavior, symlink creation), and (c) the manual npm-symlink workflow (which paths to symlink, whether `npm install` is required in the extension directory). Stop when both installation methods are fully documented from pi's package manager and extension-loading documentation.
**Decision unblocked**: Packaging the extension for distribution and writing installation documentation.

### Q9: How does `@tintinweb/pi-subagents` signal its presence or absence at runtime — what is the exact npm package name, how does module resolution behave when it is not installed (thrown error, undefined export, missing Symbol), and what patterns exist for graceful degradation short of hard failure?
**Tag**: `web`
**Covers**: `C-1 [pi-subagents must be installed as prerequisite]; NFR-2 [graceful fallback when pi-subagents absent]; C-5 [qrspi_dispatch depends on Symbol.for("pi-subagents:manager") being registered]`
**Answer shape**: A concrete answer addressing: (a) the npm package identifier, (b) the runtime behavior when `require('@tintinweb/pi-subagents')` or equivalent fails (error class, message pattern), (c) whether `Symbol.for("pi-subagents:manager")` is set synchronously at require-time or lazily, and (d) documented or observed patterns for feature-detecting pi-subagents availability and providing a user-visible fallback message instead of crashing. Stop when the detection surface is fully characterized.
**Decision unblocked**: Implementing the fallback message in `src/index.ts` when pi-subagents is not installed, and defensive checks in `qrspi_dispatch`.

### Q10: What model identifier strings and tier designations does pi-subagents recognize in agent type frontmatter (`model` field) — including how to specify haiku-tier vs sonnet-tier models — and what mechanism translates these identifiers to actual LLM API calls?
**Tag**: `web`
**Covers**: `NFR-2 [haiku-tier for reviewers/leaf agents, sonnet-tier for orchestrators]; AC-7 [extension works with multiple model tiers]`
**Answer shape**: A table of supported model identifier strings and their tier mapping (e.g., which identifiers map to haiku-tier, which to sonnet-tier), whether the `model` field in agent type frontmatter accepts shorthand tier names or requires full model IDs, and how pi-subagents resolves the model field at dispatch time (parent-agent inheritance vs explicit override). Stop when pi-subagents documentation exhausts model selection mechanics.
**Decision unblocked**: Assigning correct model values to the `model` frontmatter field across the 55 agent type files (haiku-tier for reviewers/leaf agents, sonnet-tier for orchestrators).
