Task: 01
Title: Project scaffolding and package manifest
Phase: 1
Route: full
Slice: Foundation
Dependencies: None
Scope: Create the directory structure required for development (agents/, skills/deepwork/, src/types/) and update package.json with extension metadata, pi-subagents peer dependency, and build/test scripts. Verify tsconfig.json is suitable for the extension target (CommonJS, ES2020, declaration files). Remove the existing placeholder DEEPWORK.md content since the project now has proper requirements. Ensure .gitignore covers dist/ output.
Acceptance Criteria: AC 8 (installability foundation)
NFRs: NFR: Installability (package manifest)
Gate Criteria: Phase 1 replan gate (extension loads)
Files:
  - package.json (MODIFY) — Update name, description, add @tintinweb/pi-subagents peer dependency, add test:watch script, ensure main points to dist/index.js
  - tsconfig.json (MODIFY) — Verify rootDir/outDir/module/target settings, add declarationDir if needed
  - agents/ (CREATE directory)
  - skills/deepwork/ (CREATE directory)
  - src/types/ (CREATE directory)
  - DEEPWORK.md (DELETE) — Remove placeholder content; the real spec is in .pipeline/
  - .gitignore (MODIFY) — Ensure dist/ is gitignored (already present), add .pipeline/ if missing
