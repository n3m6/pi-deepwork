Task: 24
Title: Package for distribution and install verification
Phase: 4
Route: full
Slice: Slice 4b — Integration
Dependencies: 05, 23
Scope: Verify the extension is installable via both documented methods. Test npm symlink install: run `npm run build`, symlink into `~/.pi/agent/extensions/deepwork-pi`, verify `/deepwork` command registers. Test agent type discovery: symlink `agents/` into `~/.pi/agent/agents/qrspi/`, verify agent types are discoverable. Ensure package.json has correct `main`, `files`, and repository fields for pi install compatibility. Verify the build output (`dist/`) includes all compiled TypeScript with declaration files. Add any missing `.gitignore` entries for dist artifacts.
Acceptance Criteria: AC 8 (both install methods verified)
NFRs: NFR: Installability (symlink verification)
Gate Criteria: Phase 4 replan gate (Package installable)
Files:
  - package.json (MODIFY) — Add files field for dist inclusion, verify main/scripts fields
  - .gitignore (MODIFY) — Verify dist/ is included, ensure no build artifacts leak
