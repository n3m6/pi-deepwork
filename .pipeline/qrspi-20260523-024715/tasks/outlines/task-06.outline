Task: 06
Title: Orchestrator skill (`skills/deepwork/SKILL.md`)
Phase: 1
Route: full
Slice: Foundation
Dependencies: 01
Scope: Port the 927-line opencode `deepwork.md` orchestrator prompt to pi's skill format at `skills/deepwork/SKILL.md`. Adaptations include: replacing `task` tool references with `Agent` tool dispatch, replacing `question` tool with `qrspi_question`, simplifying the telemetry system to JSONL append + run-log regeneration + metrics summary generation, removing opencode's permission system references, and adapting return contract parsing for Agent tool result output. The skill must cover all 10 pipeline stages, pre-flight setup, quick-fix route detection, backward loop protocol, resume mode, git checkpointing, and error/abort handling. This task produces the complete orchestrator prompt; later phases verify it through integration tests.
Acceptance Criteria: AC 1 (orchestrator drives pipeline), AC 2 (resume protocol), AC 3 (quick-fix route), AC 4 (backward loop), AC 5 (error handling), AC 6 (stage orchestration)
NFRs: NFR: Usability (orchestrator prompt), NFR: Observability (telemetry instructions), NFR: Performance (sequential orchestration)
Gate Criteria: Phase 1 replan gate (orchestrator skill complete)
Files:
  - skills/deepwork/SKILL.md (CREATE) — Full orchestrator prompt: pre-flight, all 10 stage dispatch tables, quick-fix detection, backward loop protocol, resume mode, git checkpointing, telemetry (events.jsonl, run-log.md, metrics-summary.md), return contract parsing, error/abort handling. Adapted from /home/n3m6/.config/opencode/agents/deepwork.md using documented conversion mappings.
